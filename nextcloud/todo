download nextcloud
extract it
mv it to {{server_dir}}
chown www-data/www-data
chmod 0644

check checksum for the archive

download if not exists.
Override nextcloud folder?
set variable to true after download
Override nextcloud database?
